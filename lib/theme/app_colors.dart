import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — Magalu Blue
  static const Color primary = Color(0xFF0086FF);
  static const Color primaryDark = Color(0xFF0060CC);
  static const Color primaryLight = Color(0xFF4DA6FF);

  // Surface & Background
  static const Color surface = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF5F5F5);
  static const Color scaffoldBackground = Color(0xFFF8F8F8);

  // Accent — Green for CTAs & positive indicators
  static const Color accentGreen = Color(0xFF00A650);
  static const Color accentGreenLight = Color(0xFFE8F5E9);

  // MagaluPay Orange
  static const Color magaluPay = Color(0xFFFF6F00);
  static const Color magaluPayLight = Color(0xFFFFF3E0);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textTertiary = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Semantic
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);
  static const Color success = Color(0xFF00A650);
  static const Color info = Color(0xFF0086FF);

  // Dividers & Borders
  static const Color divider = Color(0xFFE0E0E0);
  static const Color border = Color(0xFFEEEEEE);

  // Card shadows
  static const Color shadowLight = Color(0x0D000000);
  static const Color shadowMedium = Color(0x1A000000);

  // Shimmer
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);

  // Pix green
  static const Color pixGreen = Color(0xFF00BDAE);

  // Rating star
  static const Color ratingStar = Color(0xFFFFC107);
}
